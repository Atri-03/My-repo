# Credentials for the shared local/dev PostgreSQL instance used by docker-compose.yml.
# Override these in a local, untracked .env file for anything beyond local development.
POSTGRES_USER=platform
POSTGRES_PASSWORD=platform
POSTGRES_DB=platform
